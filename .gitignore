# Tooling / editor
node_modules/
.DS_Store
Thumbs.db
.agents/
.claude/

# Original bookmark exports (data: URIs) still embed Firebase apiKey + JWT
# from Google AI Studio. Keep them LOCAL — never push.
*.txt

# NOTE: decoded/ is the editable source for build-tools.js. The bard-injected
# Firebase blocks + credentials have been stripped from it, so it is safe to
# track and is committed (lets the repo rebuild tools/ from scratch).

# Skill lockfile (local)
skills-lock.json
